//
// Violet.Streaming
//

(function(Violet) {
  var Util = Violet.Util;

  var Streaming = function(oauth) {
    this.oauth = oauth;
  };

  Streaming.prototype = {
  };
  Violet.Streaming = Streaming;
})(Violet);
